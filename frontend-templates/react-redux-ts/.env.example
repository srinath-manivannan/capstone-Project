# Copy to ".env" to override. Without a .env the app falls back to the free
# JSONPlaceholder demo API, so the template runs with zero setup.
# Point it at a real backend by changing this one line:
VITE_API_URL=https://jsonplaceholder.typicode.com
