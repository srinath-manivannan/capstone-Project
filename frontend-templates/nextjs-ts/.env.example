# Copy to ".env.local" to override. Without it the app falls back to the free
# JSONPlaceholder demo API, so the template runs with zero setup.
# NEXT_PUBLIC_ prefix = the variable is ALSO available in the browser.
NEXT_PUBLIC_API_URL=https://jsonplaceholder.typicode.com
